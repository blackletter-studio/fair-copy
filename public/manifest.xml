<?xml version="1.0" encoding="UTF-8"?>
<!--
  THIS FILE IS A MIRROR OF ../manifest.prod.xml.

  Why it exists: Vite copies everything inside `public/` verbatim into
  `dist/`, so putting a copy of the production manifest here means every
  `pnpm build` automatically emits a ready-to-serve `dist/manifest.xml`
  that sideload users can download from:

    https://fair-copy-addin.pages.dev/manifest.xml

  DO NOT EDIT THIS FILE DIRECTLY. Edit `../manifest.prod.xml` — the
  canonical production manifest — and then copy it here verbatim. Keeping
  the two in sync is how we guarantee sideload users get the same XML we
  submit to AppSource.

  The sibling `../manifest.xml` (no `.prod.`) keeps localhost URLs for
  local `pnpm start:word` development; don't copy that one here or you'll
  ship a manifest pointing at localhost.

  Before AppSource submission, replace:
    - the placeholder icon PNGs (FC-monogram burgundy) with real branded art,
    - the GUID (currently a zero-based placeholder) with a fresh one
      generated via `uuidgen`,
    - the Version with the actual release version.
-->
<OfficeApp xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
           xmlns:ov="http://schemas.microsoft.com/office/taskpaneappversionoverrides"
           xsi:type="TaskPaneApp">
  <Id>00000000-0000-0000-0000-000000000002</Id>
  <Version>0.1.0.0</Version>
  <ProviderName>Black Letter Studio</ProviderName>
  <DefaultLocale>en-US</DefaultLocale>
  <DisplayName DefaultValue="Fair Copy" />
  <Description DefaultValue="Legal-document cleanup for Microsoft Word. Strip formatting noise, curl quotes, normalize citations, and more. Includes Proofmark — a free legal-aware proofreader." />
  <IconUrl DefaultValue="https://fair-copy-addin.pages.dev/assets/icon-80.png" />
  <HighResolutionIconUrl DefaultValue="https://fair-copy-addin.pages.dev/assets/icon-128.png" />
  <SupportUrl DefaultValue="https://blackletter.studio/support" />
  <AppDomains>
    <AppDomain>https://blackletter.studio</AppDomain>
    <AppDomain>https://fair-copy-addin.pages.dev</AppDomain>
    <AppDomain>https://fair-copy.blackletter.studio</AppDomain>
  </AppDomains>
  <Hosts>
    <Host Name="Document" />
  </Hosts>
  <Requirements>
    <Sets DefaultMinVersion="1.7">
      <Set Name="WordApi" MinVersion="1.7" />
    </Sets>
  </Requirements>
  <DefaultSettings>
    <SourceLocation DefaultValue="https://fair-copy-addin.pages.dev/src/taskpane/" />
  </DefaultSettings>
  <Permissions>ReadWriteDocument</Permissions>
</OfficeApp>
