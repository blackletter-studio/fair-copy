name: CI
on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]
jobs:
  ci:
    uses: blackletter-studio/.github/.github/workflows/reusable-node-ci.yml@main
    with:
      node-version: "20"
      run-build: true
      run-test: true
